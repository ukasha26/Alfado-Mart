events {}
http {
    include       /etc/nginx/mime.types;
    server {
        listen ${PORT};
        root /app/dist;
        index index.html;
        location / {
            try_files $uri /index.html;
        }
    }
}